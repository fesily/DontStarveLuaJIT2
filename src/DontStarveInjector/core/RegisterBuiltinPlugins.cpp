#include "RegisterBuiltinPlugins.hpp"
#include "PluginHost.hpp"

namespace ds::plugin {

void RegisterBuiltinPlugins(PluginHost &host) {
    (void) host;
    // M0: empty. Feature plugins register here in later migrations.
}

} // namespace ds::plugin
